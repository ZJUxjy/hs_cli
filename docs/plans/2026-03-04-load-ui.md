# 加载存档UI功能实现计划

> **For Claude:** 直接执行，不需要subagent

**Goal:** 完成从主菜单加载游戏存档的UI功能

**Files:**
- Modify: `src/ui/screens/MainMenu.js`

---

## 实现步骤

### 1. 在 MainMenu 添加加载存档选项

在菜单中添加"继续游戏"选项，显示已有存档列表。

### 2. 测试验证

运行: `node -e "const MainMenu = require('./src/ui/screens/MainMenu'); console.log('MainMenu loaded');"`

---

## 验收标准

1. 主菜单显示"继续游戏"选项
2. 显示存档列表（职业信息）
3. 选择存档后可加载并继续游戏
