# 发现机制实现计划

> **For Claude:** REQUIRED SUB-SKILL: Use superpowers:executing-plans to implement this plan task-by-task.

**Goal:** 实现发现(Discover)机制，从3张选项卡牌中选择1张加入手牌

**Architecture:** 在 GameEngine.js 添加发现处理逻辑，在 CardEffect.js 添加 discover 效果类型，需要前端支持选择UI

**Tech Stack:** Node.js, JSON 卡牌数据, Web API

---

## Task 1: 添加 Discover 效果类型

**Files:**
- Modify: `src/game/CardEffect.js:10-43`

**Step 1: 在 TYPES 中添加 DISCOVER 常量**

```javascript
DISCOVER: 'discover',
```

**Step 2: 测试验证**

Run: `node -e "const CardEffect = require('./src/game/CardEffect'); console.log(CardEffect.TYPES.DISCOVER);"`
Expected: 输出 "discover"

---

## Task 2: 实现发现卡牌池逻辑

**Files:**
- Create: `src/game/DiscoverPool.js`

**Step 1: 创建发现卡牌池管理类**

```javascript
// src/game/DiscoverPool.js
class DiscoverPool {
  constructor(game) {
    this.game = game;
  }

  /**
   * 获取发现选项
   * @param {string} type - 发现类型: 'spell', 'minion', 'weapon', 'class'
   * @param {string} playerClass - 玩家职业
   * @param {number} count - 选项数量，默认3
   * @returns {Array} 卡牌数组
   */
  getDiscoverOptions(type, playerClass, count = 3) {
    const CardData = require('../data/CardData');
    let pool = [];

    switch (type) {
      case 'spell':
        pool = CardData.getAllCards().filter(c => c.type === 'spell');
        break;
      case 'minion':
        pool = CardData.getAllCards().filter(c => c.type === 'minion');
        break;
      case 'weapon':
        pool = CardData.getAllCards().filter(c => c.type === 'weapon');
        break;
      case 'class':
        pool = CardData.getCardsByClass(playerClass);
        break;
      default:
        pool = CardData.getAllCards();
    }

    // 过滤掉冒险模式卡牌
    pool = pool.filter(c => !c.id.startsWith(' Adventure'));

    // 随机打乱并取前count张
    const shuffled = pool.sort(() => Math.random() - 0.5);
    return shuffled.slice(0, Math.min(count, shuffled.length));
  }
}

module.exports = DiscoverPool;
```

**Step 2: 测试验证**

Run: `node -e "const dp = require('./src/game/DiscoverPool'); console.log('DiscoverPool loaded');"`
Expected: 输出 "DiscoverPool loaded"

---

## Task 3: 添加发现效果处理方法

**Files:**
- Modify: `src/game/CardEffect.js`

**Step 1: 添加 executeDiscover 方法**

```javascript
executeDiscover(effect, context) {
  const DiscoverPool = require('./DiscoverPool');
  const discoverPool = new DiscoverPool(this.game);

  const discoverType = effect.discoverType || 'class';
  const options = discoverPool.getDiscoverOptions(
    discoverType,
    context.player.hero,
    3
  );

  // 设置待抉择状态
  this.game.state.pendingDiscover = {
    player: context.player,
    options: options,
    card: context.card,
    callback: (selectedCard) => {
      // 将选中的卡牌加入手牌
      this.game.addCardToHand(context.player, selectedCard);
      Logger.info(`${context.player.name} 发现了 ${selectedCard.name}`);
    }
  };

  return true;
}
```

**Step 2: 在 execute 方法中添加 case**

```javascript
case 'discover':
  return this.executeDiscover(effect, context);
```

**Step 3: 测试验证**

Run: `node -e "const ce = require('./src/game/CardEffect'); const c = new ce({}); console.log(typeof c.executeDiscover);"`
Expected: 输出 "function"

---

## Task 4: 在 GameEngine 添加发现处理

**Files:**
- Modify: `src/game/GameEngine.js`

**Step 1: 添加选择发现卡牌方法**

```javascript
/**
 * 选择发现的卡牌
 * @param {number} optionIndex - 选项索引 (0, 1, 或 2)
 */
selectDiscover(optionIndex) {
  if (!this.state.pendingDiscover) {
    Logger.warn('没有待发现的卡牌');
    return null;
  }

  const discover = this.state.pendingDiscover;
  if (optionIndex < 0 || optionIndex >= discover.options.length) {
    Logger.warn('无效的发现选项索引');
    return null;
  }

  const selectedCard = discover.options[optionIndex];
  discover.callback(selectedCard);
  this.state.pendingDiscover = null;

  return this.getGameState();
}
```

**Step 2: 测试验证**

Run: `node -e "const ge = require('./src/game/GameEngine'); const g = new ge(); console.log(typeof g.selectDiscover);"`
Expected: 输出 "function"

---

## Task 5: 添加发现卡牌数据

**Files:**
- Modify: `data/cards/mage.json`

**Step 1: 添加发现法术卡**

```json
{ "id": "mage_tome_of Wisdom", "name": "智慧宝珠", "type": "spell", "cost": 3, "rarity": "rare", "description": "发现一张法术牌", "effect": { "type": "discover", "discoverType": "spell" }, "classes": ["mage"] }
```

---

## Task 6: API 端点支持

**Files:**
- Modify: `src/api/game.js`

**Step 1: 添加发现选择端点**

```javascript
router.post('/discover', (req, res) => {
  const { optionIndex } = req.body;
  if (!currentGame) {
    return res.status(400).json({ error: 'No active game' });
  }
  const gameState = currentGame.selectDiscover(optionIndex);
  res.json(gameState);
});

// 获取当前发现状态
router.get('/discover', (req, res) => {
  if (!currentGame || !currentGame.state.pendingDiscover) {
    return res.json({ pending: false });
  }
  res.json({
    pending: true,
    options: currentGame.state.pendingDiscover.options.map(c => ({
      id: c.id,
      name: c.name,
      cost: c.cost,
      description: c.description
    }))
  });
});
```

**Step 2: 测试验证**

Run: `curl http://localhost:3000/api/game/discover`
Expected: 返回 {"pending": false} 或发现选项

---

## Task 7: 整体测试

**Step 1: 启动服务器测试**

Run: `node server.js &`
Expected: 服务器启动成功

**Step 2: 测试发现流程**

- 调用 /api/game/start 开始游戏
- 调用 /api/game/play 出发现卡
- 调用 /api/game/discover 选择选项
- 验证卡牌加入手牌

---

## 验收标准

1. CardEffect.js 包含 DISCOVER 类型
2. 发现卡牌可以触发3选1选择
3. 选中的卡牌加入手牌
4. API 端点支持发现流程
5. 卡牌数据包含至少1张发现卡
